# DEPRECATED -- replaced by cli/pipelines.yaml. This file is no longer
# read by cli/run or cli/setup.sh. Safe to delete; kept only because this
# environment couldn't remove it directly (filesystem permission issue on
# the mounted folder) -- please `git rm cli/pipelines.conf` yourself.
